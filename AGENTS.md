# model-training-lab 工作约定

## 开始任务

- 先阅读根 README 和相关实验的 README、配置及已有记录，保留用户正在进行的改动。
- 本机说明若存在，执行训练、安装依赖或下载资产前阅读 `硬件环境配置信息/README.md`，按其中的方式加载本地环境脚本。该目录被 Git 忽略，不随仓库分发。
- 本机说明缺失时，根据环境检查和用户已提供的信息确定资源及存储位置；单纯文档与代码分析可继续。不要把其他机器的路径或硬件假定为当前环境。

## 执行实验

- GPU 由用户指定；已有指定在其授权范围内持续有效，不重复询问。用户为后续所有实验指定的本机默认设备保存在根目录 `.local/device.json`，新实验继承该指定，具体设备身份仅写入被忽略的本机文件。用户变更统一指定时同步已有实验的 `.local/device.json`；之后若对单项实验另有指定，则按该实验最新指令执行。没有有效指定时先完成不依赖 GPU 的准备工作，启动 GPU 任务前取得指定。
- 启动前重新检查显存、进程、内存和存储空间。GPU 利用率低不代表无人占用；不自行换卡、结束他人任务或调整系统级 GPU 配置。
- 使用仓库外的 `LAB_ROOT` 保存环境、模型、数据、完整日志和检查点。环境按 `<项目>/<环境名>` 隔离，创建或同步时显式指定路径，避免默认 `.venv` 落入代码仓库。
- 安装依赖前确认解释器、包管理器缓存和临时目录。使用目标环境的 `python -m pip`，不向系统 Python 或已有无关环境安装实验依赖。
- 每次运行使用独立标识，保存命令、配置、依赖版本、种子、数据和模型版本、代码提交及未提交改动；本机记录另存实际 GPU 和设备 UUID。
- 先做小规模正确性和资源检查，再扩大训练。根据吞吐、数据等待和显存峰值调整参数，不把硬件总量直接当作独占预算。
- 对未运行、失败或未验证的步骤如实记录，不把预期结果写成实测结果。

## Git 与记录

- 每个实验按根 README 的八部分结构记录过程，保留失败分析，并提供可复现的配置和命令。
- Git 保存代码、依赖锁定文件、配置、笔记及小型结果摘要；资产和完整运行输出保存在仓库外。
- 本机硬件与环境说明仅留本地。公开文件和结果中不复制本机账号、绝对存储路径、设备 UUID、其他用户进程或凭据。
- 不将密码、密钥、真实环境凭据、模型权重、数据集、完整日志或检查点暂存、提交或推送。不强制添加被忽略的本机文件。
- 提交时按明确路径暂存并检查差异；是否提交或推送以用户当次任务的授权为准，不从远端已配置推断发布授权。
